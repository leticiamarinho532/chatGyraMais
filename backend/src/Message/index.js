const resolverMessage = require('./resolver.js');
const typeDefMessage = require('./typeDef.js');
const messageModel = require('./model.js');

module.exports =  {
    resolverMessage,
    typeDefMessage,
    messageModel
}